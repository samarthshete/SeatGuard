# Base URL of the TicketBlitz API (used for REST + Socket.io).
# Local dev defaults to http://localhost:3000 if unset.
# In production (Vercel), set this to your deployed API URL, e.g. the Render URL.
VITE_API_URL="http://localhost:3000"
