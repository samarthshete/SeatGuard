apiVersion: v1
kind: Service
metadata:
  name: ticket-blitz-api
  labels:
    app: ticket-blitz
    component: api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: ticket-blitz
    component: api
